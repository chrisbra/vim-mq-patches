# HG changeset patch
# Parent 760f81c930cc52a8c3d128339916255a8110cf4a
diff --git a/src/search.c b/src/search.c
--- a/src/search.c
+++ b/src/search.c
@@ -727,6 +727,8 @@
 					++matchcol;
 				}
 			    }
+			    if (options & SEARCH_START)
+				break;
 			    if (ptr[matchcol] == NUL
 				    || (nmatched = vim_regexec_multi(&regmatch,
 					      win, buf, lnum + matchpos.lnum,
